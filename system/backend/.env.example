PORT=3001
DATABASE_URL=postgres://postgres:postgres@localhost:5432/clinical
CORS_ORIGIN=http://localhost:5173

JWT_SECRET=change-me
JWT_EXPIRES_IN=8h

# Optional — if unset, provisioning emails are logged to the console instead of sent
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@clinical-workflow.local
